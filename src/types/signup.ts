export interface EmailResponse {
  code: string;
  message: string;
}

export interface EmailcheckResponse {
  code: string;
  message: string;
}

export interface SignupResponse {
  code: string;
  message: string;
}
